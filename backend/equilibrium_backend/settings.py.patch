--- a/backend/equilibrium_backend/settings.py
+++ b/backend/equilibrium_backend/settings.py
@@ -37,7 +37,11 @@ if isinstance(DEBUG, str):
     DEBUG = _to_bool(DEBUG)
 
-ALLOWED_HOSTS = env.list("DJANGO_ALLOWED_HOSTS", default=["localhost", "127.0.0.1", "*.up.railway.app", "*.railway.app"])
+ALLOWED_HOSTS = env.list("DJANGO_ALLOWED_HOSTS", default=["localhost", "127.0.0.1"])
+# Allow all Railway domains in production
+if not DEBUG:
+    ALLOWED_HOSTS = ["*"]
