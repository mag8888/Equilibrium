# ТЕХНИЧЕСКОЕ ЗАДАНИЕ
## Система MLM "Equilibrium" (TRINARY MLM System)

---

## 1. ОБЩЕЕ ОПИСАНИЕ ПРОЕКТА

### 1.1. Назначение
Система многоуровневого маркетинга (MLM) с трехуровневой структурой рекрутинга для управления партнерской сетью, начисления бонусов и визуализации структуры.

### 1.2. Основные характеристики
- **Название проекта**: Equilibrium MLM System (TRINARY MLM)
- **Версия**: 0.1.0
- **Платформа развертывания**: Railway.app
- **Домен**: equilibrium.up.railway.app
- **Архитектура**: Backend (Django REST API) + Frontend (React/TypeScript) + Telegram Bot

---

## 2. ТЕХНИЧЕСКИЙ СТЕК

### 2.1. Backend
- **Framework**: Django 5.1.2
- **API**: Django REST Framework 3.15.2
- **База данных**: PostgreSQL (production), SQLite (development)
- **WSGI сервер**: Gunicorn 21.2.0
- **Язык**: Python 3.12
- **Дополнительные библиотеки**:
  - `django-environ` 0.11.2 - управление переменными окружения
  - `dj-database-url` 2.2.0 - парсинг DATABASE_URL
  - `psycopg2-binary` 2.9.9 - драйвер PostgreSQL
  - `python-telegram-bot` 21.6 - интеграция с Telegram
  - `celery` 5.3.6 - асинхронные задачи
  - `redis` 5.0.1 - брокер сообщений для Celery

### 2.2. Frontend
- **Framework**: React с TypeScript
- **Build tool**: Vite
- **UI библиотеки**: Bootstrap 5, Font Awesome
- **HTTP клиент**: Fetch API / Axios

### 2.3. Инфраструктура
- **Контейнеризация**: Docker
- **CI/CD**: Railway.app (автоматический деплой из GitHub)
- **Healthcheck**: `/health/` endpoint
- **Логирование**: Gunicorn access/error logs

---

## 3. АРХИТЕКТУРА ПРОЕКТА

### 3.1. Структура директорий
```
IVA/
├── backend/                    # Основной Django проект
│   ├── equilibrium_backend/   # Настройки проекта
│   │   ├── settings.py        # Конфигурация Django
│   │   ├── urls.py            # Главный URL router
│   │   ├── wsgi.py            # WSGI приложение
│   │   └── health.py          # Healthcheck endpoint
│   ├── core/                  # Основные модели и views
│   │   ├── models.py          # User модель
│   │   ├── views.py           # Admin dashboard
│   │   └── management/        # Django команды
│   ├── mlm/                   # MLM логика
│   │   ├── models.py          # StructureNode, Tariff
│   │   └── services.py        # Размещение пользователей
│   ├── billing/               # Платежи и бонусы
│   │   ├── models.py          # Payment, Bonus
│   │   └── services.py        # Начисление бонусов
│   ├── api/                   # REST API
│   │   ├── views.py           # API endpoints
│   │   ├── serializers.py     # Сериализаторы
│   │   └── urls.py            # API routes
│   ├── integrations/          # Внешние интеграции
│   │   └── telegram.py       # Telegram bot
│   └── requirements.txt       # Python зависимости
├── frontend/                  # React приложение
├── start.sh                   # Скрипт запуска для Railway
├── Dockerfile                 # Docker образ
├── railway.json              # Конфигурация Railway
└── .dockerignore             # Исключения для Docker
```

### 3.2. Основные Django приложения

#### 3.2.1. `core`
- **Модели**: `User` (расширенная модель пользователя)
- **Функции**: Аутентификация, управление пользователями
- **Команды**: `auto_init`, `create_superuser`

#### 3.2.2. `mlm`
- **Модели**: 
  - `StructureNode` - узлы MLM структуры
  - `Tariff` - тарифные планы
- **Сервисы**: 
  - `place_user()` - размещение пользователя в структуре
  - `find_parent_for_new_partner()` - поиск родителя для размещения
  - `get_active_tariff()` - получение активного тарифа

#### 3.2.3. `billing`
- **Модели**: 
  - `Payment` - платежи пользователей
  - `Bonus` - начисленные бонусы
- **Сервисы**: 
  - `apply_signup_bonuses()` - начисление бонусов при регистрации
  - `calculate_bonus_amounts()` - расчет сумм бонусов

#### 3.2.4. `api`
- **Endpoints**: REST API для регистрации, очереди, структуры
- **Аутентификация**: Token Authentication, Session Authentication

#### 3.2.5. `integrations`
- **Telegram Bot**: Интеграция с Telegram для управления через бота

---

## 4. БИЗНЕС-ЛОГИКА MLM СИСТЕМЫ

### 4.1. Статусы пользователей
1. **Participant** (Участник) - начальный статус при регистрации
2. **Partner** (Партнер) - после оплаты вступительного взноса
3. **Admin** (Администратор) - системные администраторы

### 4.2. Тарифные планы (Tariff)
- **Поля**:
  - `code` - уникальный код тарифа (slug)
  - `name` - название тарифа
  - `entry_amount` - сумма вступительного взноса
  - `green_bonus_percent` - процент зеленого бонуса (по умолчанию 50%)
  - `yellow_bonus_percent` - процент желтого бонуса (по умолчанию 50%)
  - `is_active` - активность тарифа

### 4.3. Структура размещения
- **Максимум партнеров на уровень**: 3 (настраивается)
- **Алгоритм размещения**: 
  1. Пользователь регистрируется по реферальной ссылке
  2. Система находит пригласившего (inviter)
  3. Алгоритм BFS ищет первого пользователя с менее чем 3 партнерами
  4. Новый пользователь размещается в свободную позицию (1, 2 или 3)
  5. Уровень определяется как `parent.level + 1`

### 4.4. Система бонусов

#### 4.4.1. Зеленый бонус (Green Bonus / Payout Bonus)
- **Получатель**: Пригласивший (inviter)
- **Условие**: При регистрации нового партнера
- **Размер**: `entry_amount * green_bonus_percent / 100`
- **Пример**: При тарифе $100 и 50% = $50

#### 4.4.2. Желтый бонус (Yellow Bonus / Transition Bonus)
- **Получатель**: Владелец позиции размещения (parent)
- **Условие**: При размещении нового партнера в структуре
- **Размер**: `entry_amount * yellow_bonus_percent / 100`
- **Пример**: При тарифе $100 и 50% = $50

### 4.5. Процесс регистрации

#### Этап 1: Регистрация участника
1. Пользователь регистрируется через API `/api/register/`
2. Указывает `referral_code` пригласившего
3. Создается пользователь со статусом `PARTICIPANT`
4. Создается платеж (Payment) со статусом `PENDING`
5. Пользователь получает временный пароль

#### Этап 2: Очередь регистраций
- Администратор видит очередь через `/api/queue/`
- В очереди отображаются все пользователи с `status=PENDING`

#### Этап 3: Завершение регистрации
1. Администратор подтверждает оплату через `/api/complete/`
2. Платеж переводится в статус `COMPLETED`
3. Статус пользователя меняется на `PARTNER`
4. Пользователь размещается в структуре через `place_user()`
5. Начисляются бонусы через `apply_signup_bonuses()`

---

## 5. API ENDPOINTS

### 5.1. Healthcheck
- **URL**: `/health/`
- **Метод**: `GET`, `HEAD`
- **Аутентификация**: Не требуется
- **Описание**: Проверка работоспособности сервера
- **Ответ**: 
```json
{
  "status": "ok",
  "message": "Equilibrium MLM backend is running"
}
```

### 5.2. Статус API
- **URL**: `/api/status/`
- **Метод**: `GET`
- **Аутентификация**: Не требуется
- **Ответ**:
```json
{
  "name": "Equilibrium API",
  "version": "0.1.0",
  "status": "ok"
}
```

### 5.3. Регистрация партнера
- **URL**: `/api/register/`
- **Метод**: `POST`
- **Аутентификация**: Не требуется
- **Тело запроса**:
```json
{
  "username": "newuser",
  "email": "user@example.com",
  "referral_code": "ABC12345",
  "tariff_code": "basic" // опционально
}
```
- **Ответ** (201 Created):
```json
{
  "id": 123,
  "username": "newuser",
  "temporary_password": "random_password",
  "payment_id": 456
}
```

### 5.4. Очередь регистраций
- **URL**: `/api/queue/`
- **Метод**: `GET`
- **Аутентификация**: Требуется (Token или Session)
- **Ответ**:
```json
[
  {
    "id": 456,
    "user": 123,
    "username": "newuser",
    "inviter": "inviter_username",
    "tariff": {
      "code": "basic",
      "name": "Basic",
      "entry_amount": "100.00"
    },
    "created_at": "2024-01-15T10:30:00Z"
  }
]
```

### 5.5. Завершение регистрации
- **URL**: `/api/complete/`
- **Метод**: `POST`
- **Аутентификация**: Требуется
- **Тело запроса**:
```json
{
  "user_id": 123
}
```
- **Ответ** (200 OK):
```json
{
  "detail": "Registration completed",
  "placement_parent": "parent_username",
  "level": 2
}
```

### 5.6. Структура MLM
- **URL**: `/api/structure/`
- **Метод**: `GET`
- **Аутентификация**: Не требуется (для админ-панели)
- **Ответ**:
```json
[
  {
    "id": 1,
    "uid": "123",
    "name": "username",
    "level": 0,
    "parent_id": null,
    "tariff": "Basic",
    "position": 1
  }
]
```

### 5.7. Очередь (для админ-панели)
- **URL**: `/api/queue/`
- **Метод**: `GET`
- **Аутентификация**: Не требуется (для админ-панели)
- **Ответ**: Аналогичен `/api/queue/` но без аутентификации

### 5.8. Структура (для админ-панели)
- **URL**: `/api/structure/`
- **Метод**: `GET`
- **Аутентификация**: Не требуется (для админ-панели)

---

## 6. МОДЕЛИ ДАННЫХ

### 6.1. User (core.models.User)
Расширенная модель пользователя Django.

**Поля**:
- `username` - имя пользователя (уникальное)
- `email` - email (уникальный)
- `password` - хеш пароля
- `status` - статус: `PARTICIPANT`, `PARTNER`, `ADMIN`
- `referral_code` - уникальный реферальный код (генерируется автоматически)
- `invited_by` - ForeignKey на User (кто пригласил)
- `is_active_mlm` - активность в MLM системе

**Методы**:
- `save()` - автоматическая генерация `referral_code` при создании

### 6.2. StructureNode (mlm.models.StructureNode)
Узел MLM структуры.

**Поля**:
- `user` - OneToOneField на User
- `parent` - ForeignKey на User (родитель в структуре)
- `position` - позиция у родителя (1, 2 или 3)
- `level` - уровень в структуре (0 - корень)
- `tariff` - ForeignKey на Tariff
- `created_at` - дата создания

**Ограничения**:
- `unique_together`: (`parent`, `position`) - уникальная позиция у каждого родителя

**Свойства**:
- `children` - QuerySet дочерних узлов

### 6.3. Tariff (mlm.models.Tariff)
Тарифный план.

**Поля**:
- `code` - уникальный код (slug)
- `name` - название
- `entry_amount` - сумма вступительного взноса
- `green_bonus_percent` - процент зеленого бонуса
- `yellow_bonus_percent` - процент желтого бонуса
- `is_active` - активность

### 6.4. Payment (billing.models.Payment)
Платеж пользователя.

**Поля**:
- `user` - ForeignKey на User
- `tariff` - ForeignKey на Tariff
- `amount` - сумма платежа
- `status` - статус: `PENDING`, `COMPLETED`, `FAILED`, `CANCELLED`
- `external_id` - внешний ID платежа (для интеграций)
- `metadata` - JSON поле для дополнительных данных
- `created_at` - дата создания
- `completed_at` - дата завершения

**Методы**:
- `mark_completed()` - пометить платеж как завершенный

### 6.5. Bonus (billing.models.Bonus)
Начисленный бонус.

**Поля**:
- `user` - ForeignKey на User (получатель)
- `source_user` - ForeignKey на User (источник бонуса)
- `payment` - ForeignKey на Payment
- `bonus_type` - тип: `GREEN`, `YELLOW`
- `amount` - сумма бонуса
- `description` - описание
- `created_at` - дата начисления

---

## 7. АДМИН-ПАНЕЛЬ

### 7.1. Главная страница (`/`)
- **Функции**:
  - Дашборд с общей статистикой
  - Количество пользователей (всего, партнеров, участников)
  - Количество узлов в структуре
  - Количество ожидающих платежей
  - Общая сумма бонусов

### 7.2. Визуализация структуры (`/api/structure/`)
- **Функции**:
  - Отображение MLM структуры в виде дерева
  - Показ уровней, позиций, тарифов
  - Интерактивная визуализация (через frontend)

### 7.3. Очередь регистраций (`/api/queue/`)
- **Функции**:
  - Список пользователей, ожидающих оплаты
  - Информация о пригласившем
  - Тариф и сумма платежа
  - Кнопка завершения регистрации

### 7.4. Django Admin (`/admin/`)
- **Функции**:
  - Стандартная админ-панель Django
  - Управление всеми моделями
  - Создание суперпользователей

---

## 8. TELEGRAM ИНТЕГРАЦИЯ

### 8.1. Telegram Bot
- **Библиотека**: `python-telegram-bot` 21.6
- **Токен**: Настраивается через `TELEGRAM_BOT_TOKEN`
- **Web App URL**: Настраивается через `TELEGRAM_WEBAPP_URL`

### 8.2. Команды бота
- `/start` - приветственное сообщение
- `/app` - открытие мини-приложения с визуализацией структуры

### 8.3. Мини-приложение
- Отображает MLM структуру пользователя
- Интеграция с админ-панелью `admin_panel/structure_v2`

---

## 9. РАЗВЕРТЫВАНИЕ

### 9.1. Railway.app конфигурация

#### 9.1.1. railway.json
```json
{
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "bash start.sh",
    "healthcheckPath": "/health/",
    "healthcheckTimeout": 600,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
```

#### 9.1.2. Переменные окружения
- `DJANGO_SECRET_KEY` - секретный ключ Django
- `DJANGO_DEBUG` - режим отладки (False для production)
- `DATABASE_URL` - URL базы данных PostgreSQL
- `TELEGRAM_BOT_TOKEN` - токен Telegram бота
- `TELEGRAM_WEBAPP_URL` - URL веб-приложения для Telegram
- `RAILWAY_PUBLIC_DOMAIN` - публичный домен Railway (автоматически)

### 9.2. Docker конфигурация

#### 9.2.1. Dockerfile
- **Базовый образ**: `python:3.12-slim`
- **Рабочая директория**: `/app/backend`
- **Команда запуска**: `/app/start.sh`

#### 9.2.2. start.sh
Скрипт запуска выполняет:
1. Проверку переменных окружения
2. Очистку Python кэша
3. Создание директории staticfiles
4. Определение структуры проекта
5. Сбор статических файлов (с таймаутом)
6. Применение миграций (с таймаутом)
7. Запуск Gunicorn как основного процесса

### 9.3. Healthcheck
- **Endpoint**: `/health/`
- **Методы**: `GET`, `HEAD`
- **Таймаут**: 600 секунд (10 минут)
- **Требования**: Не требует подключения к БД, отвечает мгновенно

### 9.4. Инициализация системы

#### 9.4.1. Django команды
- `python manage.py auto_init` - автоматическая инициализация
- `python manage.py create_superuser` - создание root администратора

#### 9.4.2. API endpoint
- `POST /api/init/` - ручная инициализация через API

---

## 10. БЕЗОПАСНОСТЬ

### 10.1. Аутентификация
- **API**: Token Authentication, Session Authentication
- **Django Admin**: Стандартная аутентификация Django
- **CSRF**: Защита включена для форм

### 10.2. Разрешения
- **По умолчанию**: `AllowAny` для API (настраивается)
- **Админ-панель**: Требуется аутентификация
- **Чувствительные операции**: `IsAuthenticated`

### 10.3. Переменные окружения
- `.env` файл не обязателен (Railway использует переменные окружения)
- Проверка существования `.env` перед чтением
- Fallback на переменные окружения

---

## 11. ПРОИЗВОДИТЕЛЬНОСТЬ

### 11.1. Оптимизации
- **Отложенное подключение к БД**: `conn_max_age=600`, `connect_timeout=5`
- **Preload Gunicorn**: Загрузка приложения до форка воркеров
- **Таймауты**: Для длительных операций (collectstatic, migrate)
- **Минимальные воркеры**: 1 воркер для быстрого старта

### 11.2. Логирование
- **Gunicorn**: Access и error логи в stdout
- **Уровень логирования**: `info` (можно изменить на `debug`)
- **Захват вывода**: `--capture-output`, `--enable-stdio-inheritance`

---

## 12. ТЕСТИРОВАНИЕ

### 12.1. Локальное тестирование
```bash
# Установка зависимостей
pip install -r backend/requirements.txt

# Применение миграций
python backend/manage.py migrate

# Создание суперпользователя
python backend/manage.py create_superuser

# Запуск сервера
python backend/manage.py runserver
```

### 12.2. Тестовая сборка
- **Ветка**: `test-build`
- **Маркер**: "🧪 TEST BUILD" в логах
- **Деплой**: Настройка Railway на ветку `test-build`

---

## 13. МОНИТОРИНГ И ДИАГНОСТИКА

### 13.1. Healthcheck
- Проверка доступности сервера
- Не требует БД
- Быстрый ответ (< 100ms)

### 13.2. Логи Railway
- Доступны через панель Railway
- Показывают процесс запуска
- Ошибки Gunicorn и Django

### 13.3. Метрики
- Railway предоставляет метрики CPU, Memory, Network
- Логи запросов через Gunicorn access logs

---

## 14. РАЗРАБОТКА И ПОДДЕРЖКА

### 14.1. Git workflow
- **Основная ветка**: `main`
- **Тестовая ветка**: `test-build`
- **Репозиторий**: https://github.com/mag8888/Equilibrium

### 14.2. Правила коммитов
- Игровой проект: https://github.com/mag8888/EM2
- Бот проект: https://github.com/mag8888/EM_Bot

### 14.3. Управление версиями
- Версия API: `0.1.0`
- Версия указывается в `/api/status/`

---

## 15. БУДУЩИЕ УЛУЧШЕНИЯ

### 15.1. Планируемые функции
- [ ] Система выводов средств
- [ ] Интеграция с платежными системами
- [ ] Расширенная аналитика
- [ ] Мобильное приложение
- [ ] Многоязычность

### 15.2. Технические улучшения
- [ ] Кэширование Redis для структуры
- [ ] Асинхронные задачи через Celery
- [ ] WebSocket для real-time обновлений
- [ ] Автоматическое тестирование
- [ ] CI/CD pipeline

---

## 16. КОНТАКТЫ И ДОКУМЕНТАЦИЯ

### 16.1. Документация
- **README.md** - основная документация
- **DEPLOY_INSTRUCTIONS.md** - инструкции по развертыванию
- **RAILWAY_*.md** - документация по Railway

### 16.2. Полезные ссылки
- **Production URL**: https://equilibrium.up.railway.app
- **Healthcheck**: https://equilibrium.up.railway.app/health/
- **API Status**: https://equilibrium.up.railway.app/api/status/
- **Admin Panel**: https://equilibrium.up.railway.app/
- **Django Admin**: https://equilibrium.up.railway.app/admin/

---

## ПРИЛОЖЕНИЕ A: СХЕМА БАЗЫ ДАННЫХ

```
User (core)
├── id (PK)
├── username (unique)
├── email (unique)
├── referral_code (unique)
├── invited_by (FK -> User)
├── status (PARTICIPANT | PARTNER | ADMIN)
└── is_active_mlm

StructureNode (mlm)
├── id (PK)
├── user (OneToOne -> User)
├── parent (FK -> User)
├── position (1-3)
├── level (integer)
├── tariff (FK -> Tariff)
└── created_at

Tariff (mlm)
├── id (PK)
├── code (unique slug)
├── name
├── entry_amount
├── green_bonus_percent
├── yellow_bonus_percent
└── is_active

Payment (billing)
├── id (PK)
├── user (FK -> User)
├── tariff (FK -> Tariff)
├── amount
├── status (PENDING | COMPLETED | FAILED | CANCELLED)
├── external_id
├── metadata (JSON)
├── created_at
└── completed_at

Bonus (billing)
├── id (PK)
├── user (FK -> User) - получатель
├── source_user (FK -> User) - источник
├── payment (FK -> Payment)
├── bonus_type (GREEN | YELLOW)
├── amount
├── description
└── created_at
```

---

## ПРИЛОЖЕНИЕ B: ПРИМЕРЫ ЗАПРОСОВ

### B.1. Регистрация партнера
```bash
curl -X POST https://equilibrium.up.railway.app/api/register/ \
  -H "Content-Type: application/json" \
  -d '{
    "username": "newuser",
    "email": "user@example.com",
    "referral_code": "ABC12345"
  }'
```

### B.2. Получение очереди
```bash
curl -X GET https://equilibrium.up.railway.app/api/queue/ \
  -H "Authorization: Token YOUR_TOKEN"
```

### B.3. Завершение регистрации
```bash
curl -X POST https://equilibrium.up.railway.app/api/complete/ \
  -H "Authorization: Token YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"user_id": 123}'
```

---

**Версия документа**: 1.0  
**Дата создания**: 2024-01-15  
**Последнее обновление**: 2024-01-15

