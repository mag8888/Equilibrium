[build]
command = "pip install --no-cache-dir -r backend/requirements.txt"

[start]
command = "cd backend && python manage.py migrate --noinput && gunicorn equilibrium_backend.wsgi:application --bind 0.0.0.0:$PORT"
